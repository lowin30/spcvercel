# reglas inamovibles de spc ai:

- todo texto a base de datos: `toLowerCase()` y `sanitizeText()`.
- letra ñ: prohibido eliminarla.
- seguridad: prohibido `getSession()`, usar siempre `getUser()`.
- estilo: minúsculas siempre, sin acentos.
- tareas: referirse como titulo + id.
